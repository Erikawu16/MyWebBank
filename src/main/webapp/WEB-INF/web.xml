<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
xmlns="http://xmlns.jcp.org/xml/ns/javaee" 
xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd" 
id="WebApp_ID" version="4.0">
  <display-name>MyWebBank</display-name>
  <welcome-file-list>
    <welcome-file>index.html</welcome-file>
    <welcome-file>index.jsp</welcome-file>
    <welcome-file>index.htm</welcome-file>
    <welcome-file>default.html</welcome-file>
    <welcome-file>default.jsp</welcome-file>
    <welcome-file>default.htm</welcome-file>
  </welcome-file-list>
  
  
  
  <!-- 存放 AES Key -->
	<env-entry>
		<env-entry-name>secretKey</env-entry-name>
		<env-entry-type>java.lang.String</env-entry-type>
		<env-entry-value>0123456789abcdef0123456789abcdef</env-entry-value>
	</env-entry>

	<!-- 設定 session timeout 的時間 -->
	<session-config>
		<session-timeout>30</session-timeout>
	</session-config>
  
	<!-- 中文支援 -->
	<filter>
		<filter-name>springUtf8Encoding</filter-name>
		<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
		<init-param>
			<param-name>encoding</param-name>
			<param-value>utf-8</param-value>
		</init-param>
		<init-param>
			<param-name>forceEncoding</param-name>
			<param-value>true</param-value>
		</init-param>
	</filter>
    
    <filter-mapping>
        <filter-name>springUtf8Encoding</filter-name>
        <url-pattern>/mvc/*</url-pattern>
    </filter-mapping>

	<!-- 部署 DispatcherServlet - XML 配置版 -->
	<!-- 總的來說，這段配置指定了一個名為"springmvc"的DispatcherServlet，
	並將其映射到以"/mvc/"開頭的URL。此Servlet的初始化參數配置文件為"/WEB-INF/springmvc-servlet.xml"。
	這是一個典型的Spring MVC配置，用於處理Web應用程序中的HTTP請求和相應的分發邏輯。 -->
	<servlet>
		<servlet-name>springmvc</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<param-name>contextConfigLocation</param-name>
			<param-value>/WEB-INF/springmvc-servlet.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>springmvc</servlet-name>
		<url-pattern>/mvc/*</url-pattern>
	</servlet-mapping>


<!-- HiddenHttpMethodFilter -->
<!--此過濾器允許將HTTP方法（如PUT和DELETE）作為請求的參數發送，以克服瀏覽器僅支持GET和POST的限制。 -->
	<filter>
		<filter-name>HiddenHttpMethodFilter</filter-name>
		<filter-class>org.springframework.web.filter.HiddenHttpMethodFilter</filter-class>
	</filter>
<!-- 將HiddenHttpMethodFilter映射到與模式"/mvc/*"匹配的URL。這意味著該過濾器將應用於URL以"/mvc/"開頭的請求。 -->
	<filter-mapping>
		<filter-name>HiddenHttpMethodFilter</filter-name>
		<url-pattern>/mvc/*</url-pattern>
	</filter-mapping>


</web-app>           